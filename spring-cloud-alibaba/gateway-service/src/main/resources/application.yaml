spring:
  application:
    name: gateway-service
  config:
    import: nacos:gateway-service-dev.yml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml

